# ch2
```
寫出下列各個運算式的結果：
1.		0xFFF - 0xFAB
2.		2 / 3.0
3.		(int)2 / 3.0
4.		(int)(TRUE + TRUE)
5.		12.3 * 10 % 5
6.		'a' > 'Z'
7.		~0x0005
8.		$X = 5; echo (++$X);
9.		$Y = 10; echo ($Y--);
10.		50 > 30 ^ 70 > 100
11.		3 | 5
12.		Z = 5 > 7 ? "對" : "不對"
13.		128 << 3
14.		-4096 >> 5
15.		$Z = 10.5; var_dump($Z);
16.		echo (int)is_bool(1.0);
17.		echo (int)is_scalar('abc');
18.		echo (int)'10.5abc';
19.		echo intval(FALSE);
20.		echo (int)(("aBC" < "ABC") ^ ("abc" != "ABC"));
21.		echo(3 <=> 5);
22.		echo $a ?? $b ?? "hello";
```
# ch3
```
1.	下列程式碼在離開迴圈後，變數i的值為何？
	(a)




	(b)




	(c)



2.	撰寫一個PHP網頁，令它計算4096是2的幾次方並顯示出來。

3.	撰寫一個PHP網頁，令它根據數字1 ~ 12，在網頁上顯示對應的英文月份簡寫，
例如Jan.、Feb.、Mar.、Apr.、May.、Jun.、Jul.、Aug.、Sep.、Oct.、Nov.、Dec.。

5.	撰寫一個PHP網頁，令它計算身高為160公分、體重為65公斤的BMI，BMI等於體重 (公斤) / 身高2 (公尺2)。
若低於18.5 (不含)，就顯示「過輕」，
若介於18.5 ~ 24，就顯示「正常」，
若超過24 (不含)，就顯示「過重」，
若超過27 (不含)，就顯示「肥胖」，
若超過35 (不含)，就顯示「超極肥胖」。

```
